{
  "name": "diffhook",
  "version": "1.0.0",
  "description": "Asynchronous webhooks for monitoring changes in external webpages.",
  "main": "index.js",
  "repository": "https://github.com/brandonrninefive/diffhook.git",
  "author": "Brandon T. Ruggles",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-loader": "^7.1.2",
    "babel-preset-env": "^1.6.1",
    "babel-preset-minify": "^0.2.0",
    "http-server": "^0.10.0",
    "webpack": "^3.8.1"
  },
  "scripts": {
    "start": "npm run start",
    "build": "babel --presets=env,minify index.js -o ./build/index.min.js",
    "testBuild": "babel --presets=env,minify test.js -o ./dev/test.min.js && babel --presets=env,minify index.js -o ./dev/index.min.js",
    "test": "npm run testBuild  && node ./dev/test.min.js"
  },
  "dependencies": {
    "xmlhttprequest": "^1.8.0"
  }
}
